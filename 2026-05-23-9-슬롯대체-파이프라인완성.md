---
date: 2026-05-23
session: 9
title: Λ¹² 슬롯 대체 + 파이프라인 완성
tags: [everbluesea, dure-eta, lambda12, kosis, ecos, pipeline, slot-replace]
repo: badaegopa/-everbluesea
prev: 2026-05-23-8-η재설계-극성수정-API수집
---

# 2026-05-23 (9) Λ¹² 슬롯 대체 + 파이프라인 완성

## 한 줄 요약
재설계 변수 5종을 통합 수집(`fetch_redesign.py`)하고, 그중 구군 단위 3종(①순이동률·②재정자립도·④빈집률)을 **Λ¹² 기존 슬롯(P1·G2·C2)에 구군별로 대체 편입**. 시도/전국 단위 2종(③GRDP·⑥가계부채)은 별도 패널. CI 파이프라인 자동화 + Node 24 대응까지 완성.

## 커밋 흐름 (44ec6cc → b680dec)
| 커밋 | 내용 |
|------|------|
| `44ec6cc` | Λ¹² 편입 설계 확정 — η투입 ①②④(%변환) / 별도패널 ③⑥ |
| `38db472` | 빈집률 해석주의 `vacancy_note` |
| `71fb574` | **슬롯 대체** G2=②재정자립도, C2=④빈집률 (구군별) |
| `ec6e270` | **슬롯 대체** P1=①순이동률 (구군별, invert=True) |
| `b680dec` | 순이동률 해석주의 `migration_note` |
| (CI) | `fetch_data.yml` workflow_dispatch 3회 검증 성공 |

## 1. Λ¹² 편입 설계 확정 (`44ec6cc`)
- **η 투입 (eta_inputs, 구군 실수치)**: ① 순이동률(건수/인구×100), ② 재정자립도(그대로), ④ 빈집률(호수/총주택×100).
- **별도 패널 (separate_panel, η 제외)**: ③ 1인당 GRDP(시도), ⑥ 가계부채(전국) → `"정부 공식 데이터 없음 (시도/전국 단위만 존재)"`.
- **제약**: 가중치 소수 분해 금지 · 인구비율 분해 금지(시도/전국값 구군 미분해).
- **표시 언어**: "측정 불가" 금지. ④ 빈집률 위해 `DT_1JU1501`(총주택) 추가 수집.

## 2. Λ¹² 슬롯 대체 (핵심, `71fb574`+`ec6e270`)
`fetch_ulsan.py` calc_eta — 기존 sido 공통 슬롯 3개를 구군별 실측치로 교체.

| 슬롯 | 기존(sido 공통) | 대체(구군별) | 앵커 (lo,hi,invert) | 코드계열 |
|------|------|------|------|------|
| **P1** | 삶만족도(전국) | ① 순이동률% | (-2.0, 2.0, **True**) 유입↑=좋음 | 31계열 |
| **G2** | 수출액(국가) | ② 재정자립도% | (10, 50, True) 높을수록 좋음 | 26계열(울주26310) |
| **C2** | PM2.5(도시) | ④ 빈집률% | (3, 15, False) 높을수록 위험 | census26(울주26510) |

- 구현: ① keymap 제외 → calc_eta에서 구군별 주입. TABLES +4(순이동·재정자립도·빈집·총주택). eta 출력에 `net_migration_rate`/`fiscal_independence`/`vacancy_rate` 추가.
- **P1 방향 결정**: 앵커 invert를 유저 확정으로 `True` 채택 — 유입(+)→risk↓→η↑. 건강극성 일관(세션8 극성통일과 정합). `invert=False`면 유입이 η를 낮추는 역전이 되어 기각.
- **구군별 차등 슬롯 = 5개**: P1·S1(출산율+고령)·G2·C1(의사수)·C2. (12변수 중 5개)

### 최종 η (CI 재생성 검증, 19:03 KST)
| 구·군 | η | ①순이동률 | ②재정 | ④빈집 | 의사 |
|------|------|------|------|------|------|
| 남구 | **0.578** | -0.034 | 30.9% | 7.46% | 3.9 |
| 동구 | 0.565 | -0.093 | 17.27% | 6.82% | 3.9 |
| 북구 | 0.531 | -0.107 | 26.3% | 5.91% | 1.5 |
| 중구 | 0.497 | -0.134 | 17.79% | 6.42% | 2.1 |
| 울주군 | **0.481** | -0.101 | 37.39% | 10.18% | 1.4 |

> 울주군: 재정 최고(G2 양호)지만 빈집률 최악(10.18%)+의사 최소 → η 최하위.

## 3. 해석 주의 노트 (`38db472`+`b680dec`)
변수별 통계 함정을 데이터(`redesign.meta`)에 기록 — 패널 툴팁 노출용.
- **vacancy_note**: "재개발 추진 지역 말소 미처리 포함 가능 — 단순 쇠퇴 지표로 해석 금지"
- **migration_note**: "산단 외지 인력 유동 포함 — 정주시민 이동과 미분리. 산업 계절성 노이즈 있음. 소수 유동이 지수에 미치는 영향 제한적."

## 4. 파이프라인 완성 + CI 검증
- **순서 보장**: `fetch_data.yml` = fetch_ulsan(η 재생성) → fetch_redesign(redesign 병합) → commit. 매일 06:00 KST.
- **재생성 충돌 해소**: fetch_ulsan이 ulsan.json 통째 재생성해도 fetch_redesign이 `redesign` 블록 재부착. 슬롯 대체는 fetch_ulsan 자체에 내장돼 재생성에도 유지.
- **CI 검증 (workflow_dispatch)**: 3회 성공. 핵심 — fetch_ulsan 재생성 η가 슬롯 대체(P1/G2/C2) + 건강극성 그대로 유지됨 확인.
- **Node 24**: checkout@v5, setup-python@v6 (2026-06-02 강제전환 선제 대응).

## ★ 표별 울주군 코드 (재확인)
| 통계 계열 | 울주군 |
|------|------|
| 주민등록·이동 31계열 | `31710` |
| e-지방지표 재정 26계열 | `26310` |
| 인구주택총조사 빈집 census26 | `26510` |

## 남은 작업 (TODO)
- [ ] ⑤ data.go.kr 소방청 구급활동 OpenAPI 활용신청 → `DATA_GO_KR_API_KEY` (유일한 미해결 API)
- [ ] `fetch_lambda12.py` 미사용 수집 정리 (G2 수출액·C2 PM2.5·P1 삶만족도 — 슬롯 대체로 η 미사용)
- [ ] ① 순이동 "1인 이동건수" 한계 — 전체 인구 순이동표 보강 검토
- [ ] tools/dure_eta_v8.html · index.html 패널에 redesign 변수(순이동률·재정·빈집 + ③⑥ 별도패널 "정부 공식 데이터 없음") 표시 연동
- [x] ①②④ Λ¹² 슬롯 대체 완료 · ③⑥ 별도 패널
- [x] CI 파이프라인 자동화 + Node 24
