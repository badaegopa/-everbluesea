---
date: 2026-05-23
session: 8
title: η 재설계 — 극성 수정 + API 수집
tags: [everbluesea, dure-eta, lambda12, kosis, ecos, polarity]
repo: badaegopa/-everbluesea
---

# 2026-05-23 (8) η 재설계 — 극성 수정 + API 수집

## 한 줄 요약
DURE-η 등급 시스템을 9분위로 재설계하고, 백엔드~프런트 전 구간의 **η 극성 역전(risk↔건강)** 을 건강 극성(η↑=양호)으로 통일. 재설계용 신규 변수 5종(⑤ 소방청 제외) API 확정 + 통합 collector + 파이프라인 자동화까지 완료(API 5/6 가용).

## 커밋 흐름 (ff91ddd → ddb6730)

| 커밋 | 내용 |
|------|------|
| `ff91ddd` | η 9분위 등급 `getEtaGrade()` 추가 + `corpPenalty` 0.15→0.08 |
| `2216aae` | `CP_COLOR`/`CP_BADGE` 방향 정정 (η↑=안전) |
| `28be3d4` | η 범례 임계값 0.50→0.45 (코드 경계 일치) |
| `17cf2bf` | `corpPenalty` 0.15 원복 (정부지표 객관성) |
| `a5aac87` | **η 극성 통일** — `calc_eta` 1−x + `ulsan.json` eta값 변환 |
| `ebb722a` | 메인 `index.html` DURE-η 배너 극성 통일 (0.65/0.45) |
| `6e439df` | 배너 칩에 구군별 색상(왼쪽 보더) 반영 |
| `eef5dd3` | 경보 배너 `ETA_BASE` fallback을 ulsan.json 값으로 교체 |
| `8eec220` | 재설계 변수 6종 API 가용성 프로브 + 리포트 |
| `1d09210` | ② 울산 26계열 필터 + ⑥ ECOS 가계부채 가용 확인 |
| `bc68784` | (이 작업로그 MD 최초 작성) |
| `68df8d7` | **① 순이동 objL 3축 구조 확정** (4/6) |
| `10b5f9e` | **④ 빈집 통계표 확정** `DT_1JU1512` (5/6) |
| `274aee6` | **`fetch_redesign.py`** — 5종 통합 collector |
| `fced2e9` | redesign 5종을 `ulsan.json`에 top-level 병합 |
| `945a41f` | 파이프라인 정리 — fetch_redesign가 ulsan.json 직접 병합 |
| `ddb6730` | GitHub Actions Node 24 대응 (checkout@v5, setup-python@v6) |
| `44ec6cc` | Λ¹² 편입 설계 확정 — η투입 ①②④ / 별도패널 ③⑥ |
| `38db472` | 빈집률 해석주의 `vacancy_note` |
| `71fb574` | **Λ¹² 슬롯 대체** G2=②재정자립도, C2=④빈집률 (구군별) |
| `ec6e270` | **Λ¹² 슬롯 대체** P1=①순이동률 (구군별, invert=True) |
| `b680dec` | 순이동률 해석주의 `migration_note` |

> 이후 슬롯대체·파이프라인 완성 상세는 → `2026-05-23-9-슬롯대체-파이프라인완성.md`

## 1. η 9분위 등급 시스템 (`tools/dure_eta_v8.html`)
- `getEtaGrade(eta)` 신설: 탁월(≥0.80) → 양호 → 안정 → 평균상 → 평균(≥0.45) → 주목 → 경계 → 위험 → 위기(<0.20).
- `calcEta()` 내부 if/else 3단계 판정 → `getEtaGrade()` 호출로 교체.
- `corpPenalty` 계수: 0.15→0.08 완화 시도 후 **0.15로 원복**(정부지표 기반 객관성 유지).

## 2. η 극성 역전 발견 & 통일 (핵심)
- **문제**: 백엔드 `fetch_ulsan.py`의 `calc_eta`는 Λ¹² **risk 가중평균**을 그대로 η로 출력 → **η↑ = 위험**(risk 극성). 그러나 프런트 `getEtaGrade`/`CP_BADGE`/배너는 **η↑ = 양호**(건강 극성)로 해석.
- 결과: 의사 3.9명(최다) 남구가 화면에서 `danger`(빨강), 의료사막 울주군(1.4명)이 더 양호로 표시되는 역전.
- **원인 추적**: C1 = `천명당 의사수(역)` (`fetch_ulsan.py:37`, reverse=True). lambda12 하위값은 risk이며 0.0333은 정상(남구 의사 많음→의료위험 최저). 즉 데이터 버그 아님, **극성 컨벤션 불일치**.
- **수정 (1−x, 백엔드 단일 지점)**:
  - `fetch_ulsan.py:238` → `"eta": round(1 - score/wsm, 3)` (risk→건강 극성).
  - `data/ulsan.json` 기존 eta 5개 값 직접 1−x 변환(재생성 없이 즉시 정합).
- **프런트 정합**:
  - tools `CP_BADGE`/`CP_COLOR` 0.65/0.45 건강 극성.
  - tools η 범례 0.45/0.65.
  - `index.html` DURE-η 배너 `ETA_GREEN=0.65, ETA_YELLOW=0.45` + 마커 방향 반전.
  - `ETA_BASE` fallback eta를 ulsan.json 값으로 교체.

### 변환 후 울산 5구군 η (건강 극성)
| 구·군 | 의사/천명 | η | 비고 |
|------|------|------|------|
| 남구 | 3.9 | 0.598 | 최상위 (의료 양호) |
| 동구 | 3.9 | 0.597 | |
| 북구 | 1.5 | 0.545 | |
| 중구 | 2.1 | 0.526 | |
| 울주군 | 1.4 | 0.516 | 최하위 (의료사막) |

→ 의사 많을수록 η 높음 = 직관 일치. 4개 지점(백엔드·데이터·tools·메인) 전부 0.65/0.45 건강 극성으로 통일.

## 3. 메인 배너 칩 색상
- `chipStyle(color)` 함수화 → `border-left:3px solid {구군색상}` (`data/ulsan.json` color, fallback `#888888`).

## 4. 재설계 변수 API 확정 (`scripts/probe_redesign.py`, `fetch_redesign_report.md`)

| # | 변수 | 소스 | 상태 (최종) |
|---|------|------|------|
| ① | 순이동 | KOSIS `DT_1B26A01` | ✅ objL 3축 확정 |
| ② | 재정자립도 | KOSIS `DT_1YL20921` | ✅ 가용 (제시 `DT_1YL20711`은 오류→정정) |
| ③ | 1인당 GRDP | KOSIS `DT_1C86` | ✅ 가용 (시도단위만, 구군 분해 불가) |
| ④ | 빈집 | KOSIS `DT_1JU1512` | ✅ 가용 (호수, census 26계열) |
| ⑤ | 응급 골든타임 | data.go.kr 소방청 | ⚪ 키 미설정 (유일한 미해결) |
| ⑥ | 가계부채 | ECOS `151Y001` | ✅ 가용 (전국단위, 구군 분해 없음) |

- **② 핵심 발견**: 이 표는 31계열이 아니라 **26계열 코드** 사용 (중26010·남26020·동26030·북26040·울주26310 — `BIRTH2GU` 매핑과 동일). 재정자립도 2026: 중17.79 남30.9 동17.27 북26.3 울주37.39%.
- **⑥**: ECOS 가계신용(item `1000000`) 정상. cycle=A. 최신 2025 가계신용 **1,979조원**.

### ① DT_1B26A01 objL 3축 구조 (메타 확정 `getMeta type=ITM/TBL`)
- 표 정체 = "시군구/성/연령(5세)별 **1인 이동건수**" → 분류 3축이라 objL1만으론 "(objL) 누락" 오류.
- `itmId=T25(순이동)` + `objL1=행정구역(31계열)` + `objL2=성별 0(계)` + `objL3=연령 000(계)` + `prdSe=M`.
- ⚠️ **1인 이동건수**(건수, 명) — 순이동률(%) 아님. 율 필요시 인구로 나눔.
- 울산 순이동 2026-03: 중구 −272 / 남구 −105 / 동구 −138 / 북구 −228 / 울주군 −221명 (전 구군 순유출).

### ④ DT_1JU1512 빈집 (코드 스캔 `DT_1JU1503~1530` + 메타)
- "건축연도 및 주택종류별 미거주 주택(빈집) - 시군구". `itmId=T000(주택계)` + `objL2=00(종류계)`.
- ⚠️ **호수(건수)** — 빈집률(%)은 `DT_1JU1501`(총주택)으로 나눔.
- 울산 빈집 2024(호): 중4744 남8401 동3883 북4765 울주8901.

### ★ 울주군 코드 표별 상이 (중요 함정)
| 통계 계열 | 울주군 코드 |
|------|------|
| 주민등록·이동 (31계열) | `31710` |
| e-지방지표 재정 (26계열) | `26310` |
| 인구주택총조사 빈집 (26계열) | `26510` |
> 표마다 `getMeta`로 지역코드 확인 필수.

## 5. 통합 collector `scripts/fetch_redesign.py`
- 5종(①②③④⑥, ⑤ 제외) 한 스크립트로 수집 → `data/redesign.json`.
- 캐노니컬 키 31계열(`GU` 테이블), 표별 울주군 코드차(31710/26310/26510)를 역매핑(`REV31/REV26/REV26C`)으로 흡수.
- 레벨 구분: 구군(①②④)→`districts`, 시도(③)→`sido_ulsan`, 전국(⑥)→`national`. 항목별 try/except 격리.
- ⑥ ECOS는 시간 오름차순 반환 → `max(TIME)`으로 최신값 선택(2020 오류값 → 2025 정정).

## 6. ulsan.json 병합 + 파이프라인 자동화
- `data/redesign.json` → `ulsan.json["redesign"]` top-level 병합 (구군 31코드로 `eta`와 교차참조).
- **재생성 충돌 해소**: `fetch_redesign.py`가 ulsan.json에 직접 병합하도록 수정 + `fetch_data.yml`을 `fetch_ulsan → fetch_redesign` 순서로 정렬 → redesign이 항상 마지막에 덮어쓰므로 자동 갱신 시 사라지지 않음.
- **CI 검증**: `workflow_dispatch` 수동 2회 실행 성공. fetch_ulsan 재생성 eta가 **건강 극성 유지**(남구 0.598 최상위, 울주군 0.516 최하위) 확인 — 1−x 수정이 프로덕션 재생성에서도 적용됨.

## 7. 키/보안 + 인프라
- `ECOS_API_KEY` → **GitHub Secrets에만 저장** (`gh secret set`, 스크립트 하드코딩 X, 환경변수 주입).
- 공개 레포이므로 신규 키는 파일 미저장 원칙. KOSIS/SGIS는 기존 하드코딩 상태(레거시).
- GitHub Actions Node 24 대응: `checkout@v5`, `setup-python@v6` (2026-06-02 강제전환 선제 대응, 양 워크플로).

## 남은 작업 (TODO)
- [ ] ⑤ data.go.kr 소방청 구급활동 OpenAPI 활용신청 → `DATA_GO_KR_API_KEY` (유일한 미해결 API)
- [ ] ① 순이동 "1인 이동건수" 한계 — 전체 인구 순이동표로 보강 검토
- [ ] `fetch_lambda12.py` 미사용 수집 정리 (G2 수출액·C2 PM2.5·P1 삶만족도 — η 슬롯 대체로 미사용)
- [x] ① 순이동 objL 구조 확정 (`68df8d7`)
- [x] ④ 빈집 통계표 확정 `DT_1JU1512` (`10b5f9e`)
- [x] ①②④ Λ¹² 편입/슬롯 대체 (`71fb574`,`ec6e270`) · ③⑥ 별도패널 (`44ec6cc`) — 세션9
